installing on OSX:

  brew install pkg-config icu4c python-build epubcheck

#  brew install python-lxml
  python3 -m venv .python_venv/
  source .python_venv/bin/activate
  export PATH="/usr/local/opt/icu4c/bin:$PATH"
  export PKG_CONFIG_PATH="/opt/homebrew/opt/icu4c/lib/pkgconfig"
  pip3 install -r requirements.txt


  Install mactex: https://www.tug.org/mactex/mactex-download.html

installing on Linux:

  sudo apt-get install python3-lxml
  pip3 install -r requirements.txt

  pip uninstall pyicu
  pip install --no-binary=:pyicu: pyicu

  sudo apt-get install texlive texlive-latex-extra texlive-lang-polish
